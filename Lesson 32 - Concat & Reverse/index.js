function submit(){
    
    let names = ["John", "Jane", "Fahad", "Monica", "Sherlock"];
    let scores = [35, 90, 77, 80];
    let moreNames = ["Bill", "Steve", "Satya"];

    for (let i = 0; i < names.length; i++){
        console.log(names[i]);
    }

    document.getElementById("demo1").innerHTML = "";
}